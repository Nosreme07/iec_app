name: iec_app
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  
  # --- Internacionalização (Para deixar datas em PT-BR) ---
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2 
  # -------------------------------------------------------

  # Dependências do Firebase
  firebase_core: ^2.32.0
  firebase_auth: ^4.20.0
  cloud_firestore: ^4.17.5
  firebase_storage: ^11.7.7 
  pdf: ^3.10.4
  printing: ^5.11.0

  # Utilitários
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0 

flutter:
  uses-material-design: true

  # Configuração dos Assets (Arquivos)
  assets:
    - assets/json/ARA.json
    - assets/json/hinos.json
    # Se você for adicionar imagens depois, adicione a linha abaixo:
    # - assets/